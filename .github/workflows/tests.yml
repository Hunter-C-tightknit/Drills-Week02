# Autograder for the Week 2 drills.
#
# Runs on pull requests and on any branch except main. That is deliberate:
# the pull request is the submission, so the PR is where the green check
# belongs. Skipping main also keeps the template repo from showing a
# permanent red X — its starting state is two deliberately failing drills.
name: tests

on:
  pull_request:
  push:
    branches-ignore: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'
          cache: maven
      - name: Run the drills
        run: ./mvnw -B test
